export const richTextEffects = {
  'regular text': function (text: string): string {
    return `Regular ${text} text`;
  },
  'sarcastic text': function (text: string): string {
    return `Regular ${text} text`;
  }
};
